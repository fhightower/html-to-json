language: python
python:
  - "3.10"
  - "3.11"
  - "3.12"
  - "3.13"
install:
  - pip install uv
  - uv sync
script:
  - uv run pytest --cov=. --cov-report term-missing
  - uv run --with codecov codecov
